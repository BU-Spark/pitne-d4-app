'use strict';

/**
 * civic-association controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::civic-association.civic-association');
